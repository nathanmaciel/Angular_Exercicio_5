import { FunnyTitleDirective } from './funny-title.directive';

describe('FunnyTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FunnyTitleDirective();
    expect(directive).toBeTruthy();
  });
});
